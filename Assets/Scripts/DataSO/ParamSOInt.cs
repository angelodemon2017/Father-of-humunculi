public class ParamSOInt : ParamSO
{//TODO Why need this class
    public string Key;
    public int Value;

    public override void Init(PropsData props)
    {
//        props.Ints.AddProp(Key);
//        props.Ints.SetProp(Key, Value);
    }
}